[project]
name = "blog"
version = "0.1.0"
description = "usaturn's BLOG"
readme = "README.rst"
requires-python = ">=3.14"
dependencies = [
    "maatlog",
    "pillow",
    "sphinx",
    "sphinx-copybutton",
]

[tool.uv.sources]
maatlog = { path = "tmp/maatlog-0.0.0-py3-none-any.whl" }
