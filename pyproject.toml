[project]
name = "blog"
version = "0.1.0"
description = "usaturn's BLOG"
readme = "README.rst"
requires-python = ">=3.14"
dependencies = [
    "maatlog",
    "pillow",
    "sphinx",
    "sphinx-copybutton",
]
